//STL
queue<int> q;
q.push(3);q.front();q.back();

vector<int>v;
v.begin();v.end();v.pop_back();reverse(v.begin(),v.end());//翻转；

set<int>st;
set<int greater<int>> st;
st.insert(3);st.erase(3);st.count(3);st.clear();
for(set<int>::iterator it=st.begin();it!=st.end();it++);

string s;
s.substr(2,4);s.substr(2);s.insert(3,"123");
sort(s.begin(),s.end());
next_permutation(s.begin(),s.end());
reverse(s.begin(),s.end());

map<int string> m;
m.count(k);//1,0;
m.find(k);//迭代器;
m.erase(k);m.erase(p);m.insert(e);

next_permutation(a,a+3);
prev_permutation(a,a+3);
